spring:
  datasource:
    url: jdbc:mysql://localhost:3306/eskate?serverTimezone=UTC&useUnicode=yes&characterEncoding=UTF-8
    username: root
    password:
  liquibase:
    enabled: true
    change-log: classpath:/db/changelog/changelog-master.xml

cors:
  allowedOrigins: http://localhost:4200

springdoc:
  api-docs:
    enabled: true

server:
  port: 8080
